---
import { getCollection } from "astro:content";
import PageLayout from "@layouts/PageLayout.astro";
import TopLayout from "@layouts/TopLayout.astro";
import BottomLayout from "@layouts/BottomLayout.astro";
import AICardGeneration from "@components/kokonutui/ai-card-generation";
import { ErrorGeneration } from "@components/kokonutui/error-generation";

import { db, PromptTemplate, Comment, Author } from "astro:db";

const comments = await db.select().from(Comment);
const authors = await db.select().from(Author);
const templates = await db.select().from(PromptTemplate);

// const templates = await getCollection("promptTemplates");

console.log(templates);
---

<PageLayout title={"dev"} description={"dev"}>
  <!--<div class="bg-subwayDark background-image ">-->
  <TopLayout>
    <div class="animate page-heading">
      {"DEV"}

      <!--<ErrorGeneration error="error msg" client:only="react" />-->
    </div>
  </TopLayout>
  <BottomLayout>
    <AICardGeneration client:only="react" />
    <!--<div class="animate">-->
    <!--  <Projects client:load tags={tags} data={projects} />-->
    <!--</div>-->
  </BottomLayout>
</PageLayout>
